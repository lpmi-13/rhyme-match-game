[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "24"

# SPA fallback: let preact-router handle client-side routes on refresh / deep link.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
